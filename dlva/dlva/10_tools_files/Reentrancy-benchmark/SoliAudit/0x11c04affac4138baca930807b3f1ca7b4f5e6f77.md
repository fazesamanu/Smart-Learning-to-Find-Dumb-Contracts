# Vulnerability Analysis #
#### 2023-05-29 08:47:43 ####

* _`X`_ Underflow
    - 100%, SLOAD DIV

        - Line 49, 5 ``string public name``
        - Line 51, 5 ``string public symbol``
        - Line 52, 5 ``string public version = 'H1.0'``

* _`X`_ Overflow
    - 100%, LOG DUP PUSH AND PUSH

        - Line 77, 9 ``Approval(msg.sender, _spender, _value)``

* __O__ Multisig
* _`X`_ CallDepth
    - 100%, SWAP SWAP POP POP PUSH

        - Line 5, 5 ``function balanceOf(address _owner) constant returns (uint256 balance) {}``
        - Line 6, 5 ``function transfer(address _to, uint256 _value) returns (bool success) {}``
        - Line 7, 5 ``function transferFrom(address _from, address _to, uint256 _value) returns (bool success) {}``
        - Line 8, 5 ``function approve(address _spender, uint256 _value) returns (bool success) {}``
        - Line 9, 5 ``function allowance(address _owner, address _spender) constant returns (uint256 remaining) {}``
        - Line 14, 5 ``function transfer(address _to, uint256 _value) returns (bool success) {
        if (balances[msg.sender] >= _value && _value > 0) {
            balances[msg.sender] -= _value;
            balances[_to] += _value;
            Transfer(msg.sender, _to, _value);
            return true;
        } else { return false; }
    }``
        - Line 22, 5 ``function transferFrom(address _from, address _to, uint256 _value) returns (bool success) {
        if (balances[_from] >= _value && allowed[_from][msg.sender] >= _value && _value > 0) {
            balances[_to] += _value;
            balances[_from] -= _value;
            allowed[_from][msg.sender] -= _value;
            Transfer(_from, _to, _value);
            return true;
        } else { return false; }
    }``
        - Line 31, 5 ``function balanceOf(address _owner) constant returns (uint256 balance) {
        return balances[_owner];
    }``
        - Line 34, 5 ``function approve(address _spender, uint256 _value) returns (bool success) {
        allowed[msg.sender][_spender] = _value;
        Approval(msg.sender, _spender, _value);
        return true;
    }``
        - Line 39, 5 ``function allowance(address _owner, address _spender) constant returns (uint256 remaining) {
      return allowed[_owner][_spender];
    }``
        - Line 75, 5 ``function approveAndCall(address _spender, uint256 _value, bytes _extraData) returns (bool success) {
        allowed[msg.sender][_spender] = _value;
        Approval(msg.sender, _spender, _value);
        if(!_spender.call(bytes4(bytes32(sha3("receiveApproval(address,uint256,address,bytes)"))), msg.sender, _value, this, _extraData)) { throw; }
        return true;
    }``

* __O__ TOD
* __O__ TimeDep
* __O__ Reentrancy
* __O__ AssertFail
* __O__ TxOrigin
* __O__ CheckEffects
* _`X`_ InlineAssembly
    - 100%, PUSH GAS SUB CALL SWAP

        - Line 1, 3 ``gma solidity ^0.4.19;

contract Token {
    function totalSupply() constant returns (uint256 supply) {}
    f``

* __O__ BlockTimestamp
* _`X`_ LowlevelCalls
    - 100%, DIV CALLER

        - Line 78, 13 ``_spender.call(bytes4(bytes32(sha3("receiveApproval(address,uint256,address,bytes)"))), msg.sender, _value, this, _extraData)``

* __O__ BlockHash
* _`X`_ SelfDestruct
    - 100%, SWAP SWAP POP POP PUSH

        - Line 5, 5 ``function balanceOf(address _owner) constant returns (uint256 balance) {}``
        - Line 6, 5 ``function transfer(address _to, uint256 _value) returns (bool success) {}``
        - Line 7, 5 ``function transferFrom(address _from, address _to, uint256 _value) returns (bool success) {}``
        - Line 8, 5 ``function approve(address _spender, uint256 _value) returns (bool success) {}``
        - Line 9, 5 ``function allowance(address _owner, address _spender) constant returns (uint256 remaining) {}``
        - Line 14, 5 ``function transfer(address _to, uint256 _value) returns (bool success) {
        if (balances[msg.sender] >= _value && _value > 0) {
            balances[msg.sender] -= _value;
            balances[_to] += _value;
            Transfer(msg.sender, _to, _value);
            return true;
        } else { return false; }
    }``
        - Line 22, 5 ``function transferFrom(address _from, address _to, uint256 _value) returns (bool success) {
        if (balances[_from] >= _value && allowed[_from][msg.sender] >= _value && _value > 0) {
            balances[_to] += _value;
            balances[_from] -= _value;
            allowed[_from][msg.sender] -= _value;
            Transfer(_from, _to, _value);
            return true;
        } else { return false; }
    }``
        - Line 31, 5 ``function balanceOf(address _owner) constant returns (uint256 balance) {
        return balances[_owner];
    }``
        - Line 34, 5 ``function approve(address _spender, uint256 _value) returns (bool success) {
        allowed[msg.sender][_spender] = _value;
        Approval(msg.sender, _spender, _value);
        return true;
    }``
        - Line 39, 5 ``function allowance(address _owner, address _spender) constant returns (uint256 remaining) {
      return allowed[_owner][_spender];
    }``
        - Line 75, 5 ``function approveAndCall(address _spender, uint256 _value, bytes _extraData) returns (bool success) {
        allowed[msg.sender][_spender] = _value;
        Approval(msg.sender, _spender, _value);
        if(!_spender.call(bytes4(bytes32(sha3("receiveApproval(address,uint256,address,bytes)"))), msg.sender, _value, this, _extraData)) { throw; }
        return true;
    }``

