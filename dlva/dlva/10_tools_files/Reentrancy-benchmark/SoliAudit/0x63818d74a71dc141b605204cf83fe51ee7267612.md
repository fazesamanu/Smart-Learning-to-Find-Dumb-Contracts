# Vulnerability Analysis #
#### 2023-05-29 09:10:14 ####

* __O__ Underflow
* _`X`_ Overflow
    - 100%, SWAP SWAP SWAP SWAP SWAP

        - Line 33, 3 ``function onCreateProposal (
    bytes32 ,
    uint256 ,
    uint256 totalValueLocked,
    address ,
    uint256 proposerBalance,
    uint256 ,
    bytes calldata ,
    bytes calldata 
  ) external pure override
  {
    uint256 minProposerBalance = totalValueLocked / 100_000;
    require(
      proposerBalance >= minProposerBalance,
      'Not enough balance'
    );
  }``

* __O__ Multisig
* __O__ CallDepth
* __O__ TOD
* __O__ TimeDep
* __O__ Reentrancy
* _`X`_ AssertFail
    - 100%, DUP DUP DUP PUSH JUMPI

        - Line 60, 91 ``totalVoteCount``

* __O__ TxOrigin
* __O__ CheckEffects
* __O__ InlineAssembly
* __O__ BlockTimestamp
* _`X`_ LowlevelCalls
    - 100%, OR ISZERO

        - Line 33, 3 ``function onCreateProposal (
    bytes32 ,
    uint256 ,
    uint256 totalValueLocked,
    address ,
    uint256 proposerBalance,
    uint256 ,
    bytes calldata ,
    bytes calldata 
  ) external pure override
  {
    uint256 minProposerBalance = totalValueLocked / 100_000;
    require(
      proposerBalance >= minProposerBalance,
      'Not enough balance'
    );
  }``

* __O__ BlockHash
* __O__ SelfDestruct
