# Vulnerability Analysis #
#### 2023-05-29 08:44:32 ####

* __O__ Underflow
* _`X`_ Overflow
    - 100%, SWAP SWAP SWAP SWAP SWAP

        - Line 9, 5 ``function sendEth(address payable[] memory _users, uint[] memory _amounts) public payable {
        require(msg.sender == owner);
        require(!reentrantCheck);
        reentrantCheck = true;
        for(uint i=0;i<_users.length;i++) {
            _users[i].transfer(_amounts[i]);
        }
        delete reentrantCheck;
        if((address(this)).balance > 0) {
            owner.transfer((address(this)).balance);
        }
    }``

* __O__ Multisig
* __O__ CallDepth
* _`X`_ TOD
    - 100%, AND PUSH AND PUSH ADDRESS

        - Line 18, 13 ``owner``

* __O__ TimeDep
* __O__ Reentrancy
* __O__ AssertFail
* __O__ TxOrigin
* _`X`_ CheckEffects
    - 100%, AND SWAP POP DUP DUP

        - Line 6, 16 ``(address payable _add) public ``

* __O__ InlineAssembly
* __O__ BlockTimestamp
* _`X`_ LowlevelCalls
    - 100%, DUP CALL

        - Line 14, 13 ``_users[i].transfer(_amounts[i])``
        - Line 18, 13 ``owner.transfer((address(this)).balance)``

* __O__ BlockHash
* __O__ SelfDestruct
