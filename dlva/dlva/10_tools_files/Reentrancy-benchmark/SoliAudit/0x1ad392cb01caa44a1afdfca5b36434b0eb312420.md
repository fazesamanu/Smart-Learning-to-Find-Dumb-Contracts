# Vulnerability Analysis #
#### 2023-05-29 09:04:33 ####

* __O__ Underflow
* _`X`_ Overflow
    - 100%, PUSH JUMP JUMPDEST DUP PUSH

        - Line 13, 35 ``return;``
        - Line 17, 44 ``return;``

* __O__ Multisig
* _`X`_ CallDepth
    - 100%, SWAP SWAP POP POP PUSH

        - Line 6, 5 ``mapping (address => uint) public balances``
        - Line 12, 5 ``function mint(address receiver, uint amount) public {
        if (msg.sender != coiner) return;
        balances[receiver] += amount;
    }``
        - Line 16, 5 ``function send(address receiver, uint amount) public {
        if (balances[msg.sender] < amount) return;
        balances[msg.sender] -= amount;
        balances[receiver] += amount;
        Issue(msg.sender, receiver, amount);
    }``

* __O__ TOD
* __O__ TimeDep
* __O__ Reentrancy
* __O__ AssertFail
* __O__ TxOrigin
* __O__ CheckEffects
* __O__ InlineAssembly
* __O__ BlockTimestamp
* __O__ LowlevelCalls
* __O__ BlockHash
* _`X`_ SelfDestruct
    - 100%, SWAP SWAP POP POP PUSH

        - Line 6, 5 ``mapping (address => uint) public balances``
        - Line 12, 5 ``function mint(address receiver, uint amount) public {
        if (msg.sender != coiner) return;
        balances[receiver] += amount;
    }``
        - Line 16, 5 ``function send(address receiver, uint amount) public {
        if (balances[msg.sender] < amount) return;
        balances[msg.sender] -= amount;
        balances[receiver] += amount;
        Issue(msg.sender, receiver, amount);
    }``

