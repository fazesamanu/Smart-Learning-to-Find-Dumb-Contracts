# Vulnerability Analysis #
#### 2023-05-29 08:42:19 ####

* __O__ Underflow
* _`X`_ Overflow
    - 50%, SWAP SWAP SWAP SWAP SWAP

        - Line 5, 5 ``function deploy(bytes32 salt, bytes memory code) public returns (address) {
        uint len = code.length;
        address deployed;
        assembly{
            deployed := create2(0, add(code, 0x20), len, salt)
        }
        emit ContractDeployed(deployed);
        return deployed;
    }``

    - 50%, POP POP POP SWAP SWAP

        - Line 5, 5 ``function deploy(bytes32 salt, bytes memory code) public returns (address) {
        uint len = code.length;
        address deployed;
        assembly{
            deployed := create2(0, add(code, 0x20), len, salt)
        }
        emit ContractDeployed(deployed);
        return deployed;
    }``
        - Line 12, 9 ``return deployed``

* __O__ Multisig
* __O__ CallDepth
* __O__ TOD
* __O__ TimeDep
* __O__ Reentrancy
* __O__ AssertFail
* __O__ TxOrigin
* __O__ CheckEffects
* _`X`_ InlineAssembly
    - 100%, SWAP SWAP DUP DUP PUSH

        - Line 5, 5 ``function deploy(bytes32 salt, bytes memory code) public returns (address) {
        uint len = code.length;
        address deployed;
        assembly{
            deployed := create2(0, add(code, 0x20), len, salt)
        }
        emit ContractDeployed(deployed);
        return deployed;
    }``

* __O__ BlockTimestamp
* _`X`_ LowlevelCalls
    - 100%, PUSH CREATE

        - Line 9, 33 ``0``

* __O__ BlockHash
* __O__ SelfDestruct
