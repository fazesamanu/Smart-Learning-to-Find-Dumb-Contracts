# Vulnerability Analysis #
#### 2023-05-29 08:48:22 ####

* _`X`_ Underflow
    - 42%, GAS SUB

        - Line 200, 7 ``targetCallback.tokenFallback(from, value)``

    - 25%, PUSH GAS

        - Line 200, 7 ``targetCallback.tokenFallback(from, value)``

    - 19%, AND JUMP

        - Line 69, 28 ``balances[msg.sender].sub(_value)``
        - Line 70, 21 ``balances[_to].add(_value)``
        - Line 87, 23 ``balances[_from].sub(_value)``
        - Line 88, 21 ``balances[_to].add(_value)``
        - Line 89, 34 ``allowed[_from][msg.sender].sub(_value)``
        - Line 105, 37 ``allowed[msg.sender][_spender].add(_addedValue)``
        - Line 115, 39 ``oldValue.sub(_subtractedValue)``
        - Line 154, 21 ``totalSupply.mul(percents[wallet]).div(PERCENT_RATE)``
        - Line 154, 21 ``totalSupply.mul(percents[wallet])``
        - Line 155, 24 ``balances[this].sub(amount)``
        - Line 156, 26 ``balances[wallet].add(amount)``

    - 13%, SSTORE DUP

        - Line 124, 1 ``contract WalletsPercents is Ownable {
  address[] public wallets;
  mapping (address => uint) public percents;
  function addWallet(address wallet, uint percent) public onlyOwner {
    wallets.push(wallet);
    percents[wallet] = percent;
  }
  function cleanWallets() public onlyOwner {
    wallets.length = 0;
  }
}``
        - Line 135, 1 ``contract CommonToken is StandardToken, WalletsPercents {
  event Mint(address indexed to, uint256 amount);
  uint public constant PERCENT_RATE = 100;
  uint32 public constant decimals = 18;
  address[] public tokenHolders;
  bool public locked = false;
  mapping (address => bool)  public registeredCallbacks;
  mapping (address => bool) public unlockedAddresses;
  bool public initialized = false;
  function init() public onlyOwner {
    require(!initialized);
    totalSupply = 500000000000000000000000000;
    balances[this] = totalSupply;
    tokenHolders.push(this);
    Mint(this, totalSupply);
    unlockedAddresses[this] = true;
    unlockedAddresses[owner] = true;
    for(uint i = 0; i < wallets.length; i++) {
      address wallet = wallets[i];
      uint amount = totalSupply.mul(percents[wallet]).div(PERCENT_RATE);
      balances[this] = balances[this].sub(amount);
      balances[wallet] = balances[wallet].add(amount);
      tokenHolders.push(wallet);
      Transfer(this, wallet, amount);
    }
    initialized = true;
  }
  modifier notLocked(address sender) {
    require(!locked || unlockedAddresses[sender]);
    _;
  }
  function transferOwnership(address to) public {
    unlockedAddresses[owner] = false;
    super.transferOwnership(to);
    unlockedAddresses[owner] = true;
  }
  function addUnlockedAddress(address addressToUnlock) public onlyOwner {
    unlockedAddresses[addressToUnlock] = true;
  }
  function removeUnlockedAddress(address addressToUnlock) public onlyOwner {
    unlockedAddresses[addressToUnlock] = false;
  }
  function unlockBatchOfAddresses(address[] addressesToUnlock) public onlyOwner {
    for(uint i = 0; i < addressesToUnlock.length; i++) unlockedAddresses[addressesToUnlock[i]] = true;
  }
  function setLocked(bool newLock) public onlyOwner {
    locked = newLock;
  }
  function transfer(address to, uint256 value) public notLocked(msg.sender) returns (bool) {
    tokenHolders.push(to);
    return processCallback(super.transfer(to, value), msg.sender, to, value);
  }
  function transferFrom(address from, address to, uint256 value) public notLocked(from) returns (bool) {
    tokenHolders.push(to);
    return processCallback(super.transferFrom(from, to, value), from, to, value);
  }
  function registerCallback(address callback) public onlyOwner {
    registeredCallbacks[callback] = true;
  }
  function deregisterCallback(address callback) public onlyOwner {
    registeredCallbacks[callback] = false;
  }
  function processCallback(bool result, address from, address to, uint value) internal returns(bool) {
    if (result && registeredCallbacks[to]) {
      ReceivingContractCallback targetCallback = ReceivingContractCallback(to);
      targetCallback.tokenFallback(from, value);
    }
    return result;
  }
}``
        - Line 205, 1 ``contract BITTToken is CommonToken {
  string public constant name = "BITT";
  string public constant symbol = "BITT";
}``
        - Line 209, 1 ``contract BITZToken is CommonToken {
  string public constant name = "BITZ";
  string public constant symbol = "BITZ";
}``

* _`X`_ Overflow
    - 51%, ISZERO PUSH JUMPI JUMPDEST SWAP

        - Line 125, 3 ``address[] public wallets``
        - Line 139, 3 ``address[] public tokenHolders``
        - Line 153, 24 ``wallets[i]``
        - Line 178, 74 ``addressesToUnlock[i]``

    - 49%, JUMPDEST PUSH PUSH DUP DUP

        - Line 17, 3 ``function transferOwnership(address newOwner) public onlyOwner {
    require(newOwner != address(0));
    OwnershipTransferred(owner, newOwner);
    owner = newOwner;
  }``
        - Line 66, 3 ``function transfer(address _to, uint256 _value) public returns (bool) {
    require(_to != address(0));
    require(_value <= balances[msg.sender]);
    balances[msg.sender] = balances[msg.sender].sub(_value);
    balances[_to] = balances[_to].add(_value);
    Transfer(msg.sender, _to, _value);
    return true;
  }``
        - Line 75, 3 ``function balanceOf(address _owner) public view returns (uint256 balance) {
    return balances[_owner];
  }``
        - Line 83, 3 ``function transferFrom(address _from, address _to, uint256 _value) public returns (bool) {
    require(_to != address(0));
    require(_value <= balances[_from]);
    require(_value <= allowed[_from][msg.sender]);
    balances[_from] = balances[_from].sub(_value);
    balances[_to] = balances[_to].add(_value);
    allowed[_from][msg.sender] = allowed[_from][msg.sender].sub(_value);
    Transfer(_from, _to, _value);
    return true;
  }``
        - Line 94, 3 ``function approve(address _spender, uint256 _value) public returns (bool) {
    allowed[msg.sender][_spender] = _value;
    Approval(msg.sender, _spender, _value);
    return true;
  }``
        - Line 100, 3 ``function allowance(address _owner, address _spender) public view returns (uint256) {
    return allowed[_owner][_spender];
  }``
        - Line 104, 3 ``function increaseApproval(address _spender, uint _addedValue) public returns (bool) {
    allowed[msg.sender][_spender] = allowed[msg.sender][_spender].add(_addedValue);
    Approval(msg.sender, _spender, allowed[msg.sender][_spender]);
    return true;
  }``
        - Line 110, 3 ``function decreaseApproval(address _spender, uint _subtractedValue) public returns (bool) {
    uint oldValue = allowed[msg.sender][_spender];
    if (_subtractedValue > oldValue) {
      allowed[msg.sender][_spender] = 0;
    } else {
      allowed[msg.sender][_spender] = oldValue.sub(_subtractedValue);
    }
    Approval(msg.sender, _spender, allowed[msg.sender][_spender]);
    return true;
  }``
        - Line 125, 3 ``address[] public wallets``
        - Line 126, 3 ``mapping (address => uint) public percents``
        - Line 127, 3 ``function addWallet(address wallet, uint percent) public onlyOwner {
    wallets.push(wallet);
    percents[wallet] = percent;
  }``
        - Line 139, 3 ``address[] public tokenHolders``
        - Line 141, 3 ``mapping (address => bool)  public registeredCallbacks``
        - Line 142, 3 ``mapping (address => bool) public unlockedAddresses``
        - Line 166, 3 ``function transferOwnership(address to) public {
    unlockedAddresses[owner] = false;
    super.transferOwnership(to);
    unlockedAddresses[owner] = true;
  }``
        - Line 171, 3 ``function addUnlockedAddress(address addressToUnlock) public onlyOwner {
    unlockedAddresses[addressToUnlock] = true;
  }``
        - Line 174, 3 ``function removeUnlockedAddress(address addressToUnlock) public onlyOwner {
    unlockedAddresses[addressToUnlock] = false;
  }``
        - Line 177, 3 ``function unlockBatchOfAddresses(address[] addressesToUnlock) public onlyOwner {
    for(uint i = 0; i < addressesToUnlock.length; i++) unlockedAddresses[addressesToUnlock[i]] = true;
  }``
        - Line 180, 3 ``function setLocked(bool newLock) public onlyOwner {
    locked = newLock;
  }``
        - Line 183, 3 ``function transfer(address to, uint256 value) public notLocked(msg.sender) returns (bool) {
    tokenHolders.push(to);
    return processCallback(super.transfer(to, value), msg.sender, to, value);
  }``
        - Line 187, 3 ``function transferFrom(address from, address to, uint256 value) public notLocked(from) returns (bool) {
    tokenHolders.push(to);
    return processCallback(super.transferFrom(from, to, value), from, to, value);
  }``
        - Line 191, 3 ``function registerCallback(address callback) public onlyOwner {
    registeredCallbacks[callback] = true;
  }``
        - Line 194, 3 ``function deregisterCallback(address callback) public onlyOwner {
    registeredCallbacks[callback] = false;
  }``

* __O__ Multisig
* _`X`_ CallDepth
    - 100%, SWAP SWAP POP POP PUSH

        - Line 17, 3 ``function transferOwnership(address newOwner) public onlyOwner {
    require(newOwner != address(0));
    OwnershipTransferred(owner, newOwner);
    owner = newOwner;
  }``
        - Line 66, 3 ``function transfer(address _to, uint256 _value) public returns (bool) {
    require(_to != address(0));
    require(_value <= balances[msg.sender]);
    balances[msg.sender] = balances[msg.sender].sub(_value);
    balances[_to] = balances[_to].add(_value);
    Transfer(msg.sender, _to, _value);
    return true;
  }``
        - Line 75, 3 ``function balanceOf(address _owner) public view returns (uint256 balance) {
    return balances[_owner];
  }``
        - Line 83, 3 ``function transferFrom(address _from, address _to, uint256 _value) public returns (bool) {
    require(_to != address(0));
    require(_value <= balances[_from]);
    require(_value <= allowed[_from][msg.sender]);
    balances[_from] = balances[_from].sub(_value);
    balances[_to] = balances[_to].add(_value);
    allowed[_from][msg.sender] = allowed[_from][msg.sender].sub(_value);
    Transfer(_from, _to, _value);
    return true;
  }``
        - Line 94, 3 ``function approve(address _spender, uint256 _value) public returns (bool) {
    allowed[msg.sender][_spender] = _value;
    Approval(msg.sender, _spender, _value);
    return true;
  }``
        - Line 100, 3 ``function allowance(address _owner, address _spender) public view returns (uint256) {
    return allowed[_owner][_spender];
  }``
        - Line 104, 3 ``function increaseApproval(address _spender, uint _addedValue) public returns (bool) {
    allowed[msg.sender][_spender] = allowed[msg.sender][_spender].add(_addedValue);
    Approval(msg.sender, _spender, allowed[msg.sender][_spender]);
    return true;
  }``
        - Line 110, 3 ``function decreaseApproval(address _spender, uint _subtractedValue) public returns (bool) {
    uint oldValue = allowed[msg.sender][_spender];
    if (_subtractedValue > oldValue) {
      allowed[msg.sender][_spender] = 0;
    } else {
      allowed[msg.sender][_spender] = oldValue.sub(_subtractedValue);
    }
    Approval(msg.sender, _spender, allowed[msg.sender][_spender]);
    return true;
  }``
        - Line 125, 3 ``address[] public wallets``
        - Line 126, 3 ``mapping (address => uint) public percents``
        - Line 127, 3 ``function addWallet(address wallet, uint percent) public onlyOwner {
    wallets.push(wallet);
    percents[wallet] = percent;
  }``
        - Line 139, 3 ``address[] public tokenHolders``
        - Line 141, 3 ``mapping (address => bool)  public registeredCallbacks``
        - Line 142, 3 ``mapping (address => bool) public unlockedAddresses``
        - Line 166, 3 ``function transferOwnership(address to) public {
    unlockedAddresses[owner] = false;
    super.transferOwnership(to);
    unlockedAddresses[owner] = true;
  }``
        - Line 171, 3 ``function addUnlockedAddress(address addressToUnlock) public onlyOwner {
    unlockedAddresses[addressToUnlock] = true;
  }``
        - Line 174, 3 ``function removeUnlockedAddress(address addressToUnlock) public onlyOwner {
    unlockedAddresses[addressToUnlock] = false;
  }``
        - Line 177, 3 ``function unlockBatchOfAddresses(address[] addressesToUnlock) public onlyOwner {
    for(uint i = 0; i < addressesToUnlock.length; i++) unlockedAddresses[addressesToUnlock[i]] = true;
  }``
        - Line 180, 3 ``function setLocked(bool newLock) public onlyOwner {
    locked = newLock;
  }``
        - Line 183, 3 ``function transfer(address to, uint256 value) public notLocked(msg.sender) returns (bool) {
    tokenHolders.push(to);
    return processCallback(super.transfer(to, value), msg.sender, to, value);
  }``
        - Line 187, 3 ``function transferFrom(address from, address to, uint256 value) public notLocked(from) returns (bool) {
    tokenHolders.push(to);
    return processCallback(super.transferFrom(from, to, value), from, to, value);
  }``
        - Line 191, 3 ``function registerCallback(address callback) public onlyOwner {
    registeredCallbacks[callback] = true;
  }``
        - Line 194, 3 ``function deregisterCallback(address callback) public onlyOwner {
    registeredCallbacks[callback] = false;
  }``

* __O__ TOD
* __O__ TimeDep
* __O__ Reentrancy
* _`X`_ AssertFail
    - 100%, PUSH JUMP JUMPDEST SWAP PUSH

        - Line 128, 5 ``wallets.push(wallet)``
        - Line 148, 5 ``tokenHolders.push(this)``
        - Line 157, 7 ``tokenHolders.push(wallet)``
        - Line 184, 5 ``tokenHolders.push(to)``
        - Line 188, 5 ``tokenHolders.push(to)``

* __O__ TxOrigin
* __O__ CheckEffects
* __O__ InlineAssembly
* __O__ BlockTimestamp
* _`X`_ LowlevelCalls
    - 53%, CALL ISZERO

        - Line 200, 7 ``targetCallback.tokenFallback(from, value)``

    - 47%, SWAP ADD

        - Line 124, 1 ``contract WalletsPercents is Ownable {
  address[] public wallets;
  mapping (address => uint) public percents;
  function addWallet(address wallet, uint percent) public onlyOwner {
    wallets.push(wallet);
    percents[wallet] = percent;
  }
  function cleanWallets() public onlyOwner {
    wallets.length = 0;
  }
}``
        - Line 125, 3 ``address[] public wallets``
        - Line 128, 5 ``wallets.push(wallet)``
        - Line 135, 1 ``contract CommonToken is StandardToken, WalletsPercents {
  event Mint(address indexed to, uint256 amount);
  uint public constant PERCENT_RATE = 100;
  uint32 public constant decimals = 18;
  address[] public tokenHolders;
  bool public locked = false;
  mapping (address => bool)  public registeredCallbacks;
  mapping (address => bool) public unlockedAddresses;
  bool public initialized = false;
  function init() public onlyOwner {
    require(!initialized);
    totalSupply = 500000000000000000000000000;
    balances[this] = totalSupply;
    tokenHolders.push(this);
    Mint(this, totalSupply);
    unlockedAddresses[this] = true;
    unlockedAddresses[owner] = true;
    for(uint i = 0; i < wallets.length; i++) {
      address wallet = wallets[i];
      uint amount = totalSupply.mul(percents[wallet]).div(PERCENT_RATE);
      balances[this] = balances[this].sub(amount);
      balances[wallet] = balances[wallet].add(amount);
      tokenHolders.push(wallet);
      Transfer(this, wallet, amount);
    }
    initialized = true;
  }
  modifier notLocked(address sender) {
    require(!locked || unlockedAddresses[sender]);
    _;
  }
  function transferOwnership(address to) public {
    unlockedAddresses[owner] = false;
    super.transferOwnership(to);
    unlockedAddresses[owner] = true;
  }
  function addUnlockedAddress(address addressToUnlock) public onlyOwner {
    unlockedAddresses[addressToUnlock] = true;
  }
  function removeUnlockedAddress(address addressToUnlock) public onlyOwner {
    unlockedAddresses[addressToUnlock] = false;
  }
  function unlockBatchOfAddresses(address[] addressesToUnlock) public onlyOwner {
    for(uint i = 0; i < addressesToUnlock.length; i++) unlockedAddresses[addressesToUnlock[i]] = true;
  }
  function setLocked(bool newLock) public onlyOwner {
    locked = newLock;
  }
  function transfer(address to, uint256 value) public notLocked(msg.sender) returns (bool) {
    tokenHolders.push(to);
    return processCallback(super.transfer(to, value), msg.sender, to, value);
  }
  function transferFrom(address from, address to, uint256 value) public notLocked(from) returns (bool) {
    tokenHolders.push(to);
    return processCallback(super.transferFrom(from, to, value), from, to, value);
  }
  function registerCallback(address callback) public onlyOwner {
    registeredCallbacks[callback] = true;
  }
  function deregisterCallback(address callback) public onlyOwner {
    registeredCallbacks[callback] = false;
  }
  function processCallback(bool result, address from, address to, uint value) internal returns(bool) {
    if (result && registeredCallbacks[to]) {
      ReceivingContractCallback targetCallback = ReceivingContractCallback(to);
      targetCallback.tokenFallback(from, value);
    }
    return result;
  }
}``
        - Line 139, 3 ``address[] public tokenHolders``
        - Line 148, 5 ``tokenHolders.push(this)``
        - Line 153, 24 ``wallets[i]``
        - Line 157, 7 ``tokenHolders.push(wallet)``
        - Line 184, 5 ``tokenHolders.push(to)``
        - Line 188, 5 ``tokenHolders.push(to)``
        - Line 205, 1 ``contract BITTToken is CommonToken {
  string public constant name = "BITT";
  string public constant symbol = "BITT";
}``
        - Line 209, 1 ``contract BITZToken is CommonToken {
  string public constant name = "BITZ";
  string public constant symbol = "BITZ";
}``

* __O__ BlockHash
* _`X`_ SelfDestruct
    - 100%, SWAP SWAP POP POP PUSH

        - Line 17, 3 ``function transferOwnership(address newOwner) public onlyOwner {
    require(newOwner != address(0));
    OwnershipTransferred(owner, newOwner);
    owner = newOwner;
  }``
        - Line 66, 3 ``function transfer(address _to, uint256 _value) public returns (bool) {
    require(_to != address(0));
    require(_value <= balances[msg.sender]);
    balances[msg.sender] = balances[msg.sender].sub(_value);
    balances[_to] = balances[_to].add(_value);
    Transfer(msg.sender, _to, _value);
    return true;
  }``
        - Line 75, 3 ``function balanceOf(address _owner) public view returns (uint256 balance) {
    return balances[_owner];
  }``
        - Line 83, 3 ``function transferFrom(address _from, address _to, uint256 _value) public returns (bool) {
    require(_to != address(0));
    require(_value <= balances[_from]);
    require(_value <= allowed[_from][msg.sender]);
    balances[_from] = balances[_from].sub(_value);
    balances[_to] = balances[_to].add(_value);
    allowed[_from][msg.sender] = allowed[_from][msg.sender].sub(_value);
    Transfer(_from, _to, _value);
    return true;
  }``
        - Line 94, 3 ``function approve(address _spender, uint256 _value) public returns (bool) {
    allowed[msg.sender][_spender] = _value;
    Approval(msg.sender, _spender, _value);
    return true;
  }``
        - Line 100, 3 ``function allowance(address _owner, address _spender) public view returns (uint256) {
    return allowed[_owner][_spender];
  }``
        - Line 104, 3 ``function increaseApproval(address _spender, uint _addedValue) public returns (bool) {
    allowed[msg.sender][_spender] = allowed[msg.sender][_spender].add(_addedValue);
    Approval(msg.sender, _spender, allowed[msg.sender][_spender]);
    return true;
  }``
        - Line 110, 3 ``function decreaseApproval(address _spender, uint _subtractedValue) public returns (bool) {
    uint oldValue = allowed[msg.sender][_spender];
    if (_subtractedValue > oldValue) {
      allowed[msg.sender][_spender] = 0;
    } else {
      allowed[msg.sender][_spender] = oldValue.sub(_subtractedValue);
    }
    Approval(msg.sender, _spender, allowed[msg.sender][_spender]);
    return true;
  }``
        - Line 125, 3 ``address[] public wallets``
        - Line 126, 3 ``mapping (address => uint) public percents``
        - Line 127, 3 ``function addWallet(address wallet, uint percent) public onlyOwner {
    wallets.push(wallet);
    percents[wallet] = percent;
  }``
        - Line 139, 3 ``address[] public tokenHolders``
        - Line 141, 3 ``mapping (address => bool)  public registeredCallbacks``
        - Line 142, 3 ``mapping (address => bool) public unlockedAddresses``
        - Line 166, 3 ``function transferOwnership(address to) public {
    unlockedAddresses[owner] = false;
    super.transferOwnership(to);
    unlockedAddresses[owner] = true;
  }``
        - Line 171, 3 ``function addUnlockedAddress(address addressToUnlock) public onlyOwner {
    unlockedAddresses[addressToUnlock] = true;
  }``
        - Line 174, 3 ``function removeUnlockedAddress(address addressToUnlock) public onlyOwner {
    unlockedAddresses[addressToUnlock] = false;
  }``
        - Line 177, 3 ``function unlockBatchOfAddresses(address[] addressesToUnlock) public onlyOwner {
    for(uint i = 0; i < addressesToUnlock.length; i++) unlockedAddresses[addressesToUnlock[i]] = true;
  }``
        - Line 180, 3 ``function setLocked(bool newLock) public onlyOwner {
    locked = newLock;
  }``
        - Line 183, 3 ``function transfer(address to, uint256 value) public notLocked(msg.sender) returns (bool) {
    tokenHolders.push(to);
    return processCallback(super.transfer(to, value), msg.sender, to, value);
  }``
        - Line 187, 3 ``function transferFrom(address from, address to, uint256 value) public notLocked(from) returns (bool) {
    tokenHolders.push(to);
    return processCallback(super.transferFrom(from, to, value), from, to, value);
  }``
        - Line 191, 3 ``function registerCallback(address callback) public onlyOwner {
    registeredCallbacks[callback] = true;
  }``
        - Line 194, 3 ``function deregisterCallback(address callback) public onlyOwner {
    registeredCallbacks[callback] = false;
  }``

