# Vulnerability Analysis #
#### 2023-05-29 09:16:41 ####

* __O__ Underflow
* _`X`_ Overflow
    - 58%, SWAP SWAP SWAP SWAP SWAP

        - Line 4, 5 ``function deployCall(bytes memory contractCode, bytes memory callCode) external returns (bytes memory resultData) {
        address addr;
        assembly {
            addr := create(0, add(contractCode, 0x20), mload(contractCode))
        }
        (, resultData) = addr.call(callCode);
    }``

    - 42%, POP POP POP SWAP SWAP

        - Line 4, 5 ``function deployCall(bytes memory contractCode, bytes memory callCode) external returns (bytes memory resultData) {
        address addr;
        assembly {
            addr := create(0, add(contractCode, 0x20), mload(contractCode))
        }
        (, resultData) = addr.call(callCode);
    }``
        - Line 9, 9 ``(, resultData) = addr.call(callCode)``

* __O__ Multisig
* __O__ CallDepth
* __O__ TOD
* __O__ TimeDep
* __O__ Reentrancy
* _`X`_ AssertFail
    - 100%, SWAP PUSH DUP GT ISZERO

        - Line 4, 5 ``function deployCall(bytes memory contractCode, bytes memory callCode) external returns (bytes memory resultData) {
        address addr;
        assembly {
            addr := create(0, add(contractCode, 0x20), mload(contractCode))
        }
        (, resultData) = addr.call(callCode);
    }``

* __O__ TxOrigin
* _`X`_ CheckEffects
    - 100%, PUSH DUP MLOAD PUSH DUP

        - Line 5, 9 ``address addr``

* _`X`_ InlineAssembly
    - 100%, PUSH DUP MLOAD PUSH DUP

        - Line 5, 9 ``address addr``

* __O__ BlockTimestamp
* _`X`_ LowlevelCalls
    - 100%, OR ISZERO

        - Line 4, 5 ``function deployCall(bytes memory contractCode, bytes memory callCode) external returns (bytes memory resultData) {
        address addr;
        assembly {
            addr := create(0, add(contractCode, 0x20), mload(contractCode))
        }
        (, resultData) = addr.call(callCode);
    }``

* __O__ BlockHash
* __O__ SelfDestruct
