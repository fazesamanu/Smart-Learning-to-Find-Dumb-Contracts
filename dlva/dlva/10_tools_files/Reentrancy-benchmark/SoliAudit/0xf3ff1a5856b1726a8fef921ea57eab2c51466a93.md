# Vulnerability Analysis #
#### 2023-05-29 08:50:59 ####

* _`X`_ Underflow
    - 100%, ADD SHA

        - Line 12, 16 ``keccak256(encoded)``

* _`X`_ Overflow
    - 100%, SWAP SWAP SWAP SWAP SWAP

        - Line 4, 5 ``function encodeClaim(
        address[] calldata tokens,
        uint256[] calldata cumulativeAmounts,
        address account,
        uint256 index,
        uint256 cycle
    ) public view returns (bytes memory encoded, bytes32 hash) {
        encoded = abi.encode(index, account, cycle, tokens, cumulativeAmounts);
        hash = keccak256(encoded);
    }``

* __O__ Multisig
* __O__ CallDepth
* _`X`_ TOD
    - 100%, DUP DUP SUB SUB DUP

        - Line 11, 19 ``abi.encode(index, account, cycle, tokens, cumulativeAmounts)``

* __O__ TimeDep
* __O__ Reentrancy
* _`X`_ AssertFail
    - 100%, REVERT JUMPDEST SWAP SWAP SWAP

        - Line 4, 5 ``function encodeClaim(
        address[] calldata tokens,
        uint256[] calldata cumulativeAmounts,
        address account,
        uint256 index,
        uint256 cycle
    ) public view returns (bytes memory encoded, bytes32 hash) {
        encoded = abi.encode(index, account, cycle, tokens, cumulativeAmounts);
        hash = keccak256(encoded);
    }``

* __O__ TxOrigin
* __O__ CheckEffects
* __O__ InlineAssembly
* __O__ BlockTimestamp
* __O__ LowlevelCalls
* __O__ BlockHash
* __O__ SelfDestruct
