# Vulnerability Analysis #
#### 2023-05-29 08:44:27 ####

* _`X`_ Underflow
    - 100%, GAS SUB

        - Line 1, 3 ``gma solidity ^0.4.18;

contract Ownable {
  address public owner;
  event OwnershipTransferred(address indexe``
        - Line 40, 17 ``t.transferFrom(from, this, value)``
        - Line 77, 52 ``registry.contracts(msg.sender)``
        - Line 144, 17 ``registry.contracts(msg.sender)``
        - Line 145, 16 ``ERC20(token).transferFrom(from, to, amount)``

* _`X`_ Overflow
    - 100%, SWAP DUP ADD DUP CALLDATALOAD

        - Line 38, 5 ``function receiveApproval(address from, uint256 value, address token, bytes extraData) public {
        ERC20 t = ERC20(token);
        require(t.transferFrom(from, this, value));
        ReceivedTokens(from, value, token, extraData);
    }``
        - Line 73, 5 ``function proxy(address dest, HowToCall howToCall, bytes calldata)
        public
        returns (bool result)
    {
        require(msg.sender == user || (!revoked && registry.contracts(msg.sender)));
        if (howToCall == HowToCall.Call) {
            result = dest.call(calldata);
        } else if (howToCall == HowToCall.DelegateCall) {
            result = dest.delegatecall(calldata);
        }
        return result;
    }``
        - Line 86, 5 ``function proxyAssert(address dest, HowToCall howToCall, bytes calldata)
        public
    {
        require(proxy(dest, howToCall, calldata));
    }``

* __O__ Multisig
* _`X`_ CallDepth
    - 100%, SWAP SWAP POP POP PUSH

        - Line 16, 3 ``function transferOwnership(address newOwner) public onlyOwner {
    require(newOwner != address(0));
    OwnershipTransferred(owner, newOwner);
    owner = newOwner;
  }``
        - Line 38, 5 ``function receiveApproval(address from, uint256 value, address token, bytes extraData) public {
        ERC20 t = ERC20(token);
        require(t.transferFrom(from, this, value));
        ReceivedTokens(from, value, token, extraData);
    }``
        - Line 65, 5 ``function setRevoke(bool revoke)
        public
    {
        require(msg.sender == user);
        revoked = revoke;
        Revoked(revoke);
    }``
        - Line 73, 5 ``function proxy(address dest, HowToCall howToCall, bytes calldata)
        public
        returns (bool result)
    {
        require(msg.sender == user || (!revoked && registry.contracts(msg.sender)));
        if (howToCall == HowToCall.Call) {
            result = dest.call(calldata);
        } else if (howToCall == HowToCall.DelegateCall) {
            result = dest.delegatecall(calldata);
        }
        return result;
    }``
        - Line 86, 5 ``function proxyAssert(address dest, HowToCall howToCall, bytes calldata)
        public
    {
        require(proxy(dest, howToCall, calldata));
    }``
        - Line 94, 5 ``mapping(address => AuthenticatedProxy) public proxies``
        - Line 96, 5 ``mapping(address => uint) public pending``
        - Line 98, 5 ``mapping(address => bool) public contracts``
        - Line 102, 5 ``function startGrantAuthentication (address addr)
        public
        onlyOwner
    {
        require(!contracts[addr] && pending[addr] == 0);
        pending[addr] = now;
    }``
        - Line 110, 5 ``function endGrantAuthentication (address addr)
        public
        onlyOwner
    {
        require(!contracts[addr] && pending[addr] != 0 && ((pending[addr] + DELAY_PERIOD) < now));
        pending[addr] = 0;
        contracts[addr] = true;
    }``
        - Line 119, 5 ``function revokeAuthentication (address addr)
        public
        onlyOwner
    {
        contracts[addr] = false;
    }``
        - Line 140, 5 ``function transferFrom(address token, address from, address to, uint amount)
        public
        returns (bool)
    {
        require(registry.contracts(msg.sender));
        return ERC20(token).transferFrom(from, to, amount);
    }``

* __O__ TOD
* __O__ TimeDep
* __O__ Reentrancy
* __O__ AssertFail
* __O__ TxOrigin
* __O__ CheckEffects
* _`X`_ InlineAssembly
    - 100%, PUSH GAS SUB CALL SWAP

        - Line 1, 3 ``gma solidity ^0.4.18;

contract Ownable {
  address public owner;
  event OwnershipTransferred(address indexe``

* _`X`_ BlockTimestamp
    - 100%, TIMESTAMP

        - Line 107, 25 ``now``
        - Line 114, 93 ``now``

* _`X`_ LowlevelCalls
    - 100%, CALL ISZERO

        - Line 40, 17 ``t.transferFrom(from, this, value)``
        - Line 77, 52 ``registry.contracts(msg.sender)``
        - Line 144, 17 ``registry.contracts(msg.sender)``
        - Line 145, 16 ``ERC20(token).transferFrom(from, to, amount)``

* __O__ BlockHash
* _`X`_ SelfDestruct
    - 100%, SWAP SWAP POP POP PUSH

        - Line 16, 3 ``function transferOwnership(address newOwner) public onlyOwner {
    require(newOwner != address(0));
    OwnershipTransferred(owner, newOwner);
    owner = newOwner;
  }``
        - Line 38, 5 ``function receiveApproval(address from, uint256 value, address token, bytes extraData) public {
        ERC20 t = ERC20(token);
        require(t.transferFrom(from, this, value));
        ReceivedTokens(from, value, token, extraData);
    }``
        - Line 65, 5 ``function setRevoke(bool revoke)
        public
    {
        require(msg.sender == user);
        revoked = revoke;
        Revoked(revoke);
    }``
        - Line 73, 5 ``function proxy(address dest, HowToCall howToCall, bytes calldata)
        public
        returns (bool result)
    {
        require(msg.sender == user || (!revoked && registry.contracts(msg.sender)));
        if (howToCall == HowToCall.Call) {
            result = dest.call(calldata);
        } else if (howToCall == HowToCall.DelegateCall) {
            result = dest.delegatecall(calldata);
        }
        return result;
    }``
        - Line 86, 5 ``function proxyAssert(address dest, HowToCall howToCall, bytes calldata)
        public
    {
        require(proxy(dest, howToCall, calldata));
    }``
        - Line 94, 5 ``mapping(address => AuthenticatedProxy) public proxies``
        - Line 96, 5 ``mapping(address => uint) public pending``
        - Line 98, 5 ``mapping(address => bool) public contracts``
        - Line 102, 5 ``function startGrantAuthentication (address addr)
        public
        onlyOwner
    {
        require(!contracts[addr] && pending[addr] == 0);
        pending[addr] = now;
    }``
        - Line 110, 5 ``function endGrantAuthentication (address addr)
        public
        onlyOwner
    {
        require(!contracts[addr] && pending[addr] != 0 && ((pending[addr] + DELAY_PERIOD) < now));
        pending[addr] = 0;
        contracts[addr] = true;
    }``
        - Line 119, 5 ``function revokeAuthentication (address addr)
        public
        onlyOwner
    {
        contracts[addr] = false;
    }``
        - Line 140, 5 ``function transferFrom(address token, address from, address to, uint amount)
        public
        returns (bool)
    {
        require(registry.contracts(msg.sender));
        return ERC20(token).transferFrom(from, to, amount);
    }``

