# Vulnerability Analysis #
#### 2023-05-29 08:52:45 ####

* _`X`_ Underflow
    - 100%, SUB DELEGATECALL

        - Line 81, 32 ``balances[msg.sender].sub(tokens)``
        - Line 82, 24 ``balances[to].add(tokens)``
        - Line 92, 26 ``balances[from].sub(tokens)``
        - Line 93, 37 ``allowed[from][msg.sender].sub(tokens)``
        - Line 94, 24 ``balances[to].add(tokens)``
        - Line 115, 32 ``balances[msg.sender].add(tokens)``
        - Line 116, 24 ``_totalSupply.add(tokens)``

* __O__ Overflow
* __O__ Multisig
* _`X`_ CallDepth
    - 100%, SWAP SWAP POP POP PUSH

        - Line 4, 5 ``function add(uint a, uint b) public pure returns (uint c) {
        c = a + b;
        require(c >= a);
    }``
        - Line 8, 5 ``function sub(uint a, uint b) public pure returns (uint c) {
        require(b <= a);
        c = a - b;
    }``
        - Line 12, 5 ``function mul(uint a, uint b) public pure returns (uint c) {
        c = a * b;
        require(a == 0 || c / a == b);
    }``
        - Line 16, 5 ``function div(uint a, uint b) public pure returns (uint c) {
        require(b > 0);
        c = a / b;
    }``
        - Line 45, 5 ``function transferOwnership(address _newOwner) public onlyOwner {
        newOwner = _newOwner;
    }``
        - Line 77, 5 ``function balanceOf(address tokenOwner) public constant returns (uint balance) {
        return balances[tokenOwner];
    }``
        - Line 80, 5 ``function transfer(address to, uint tokens) public returns (bool success) {
        balances[msg.sender] = balances[msg.sender].sub(tokens);
        balances[to] = balances[to].add(tokens);
        Transfer(msg.sender, to, tokens);
        return true;
    }``
        - Line 86, 5 ``function approve(address spender, uint tokens) public returns (bool success) {
        allowed[msg.sender][spender] = tokens;
        Approval(msg.sender, spender, tokens);
        return true;
    }``
        - Line 91, 5 ``function transferFrom(address from, address to, uint tokens) public returns (bool success) {
        balances[from] = balances[from].sub(tokens);
        allowed[from][msg.sender] = allowed[from][msg.sender].sub(tokens);
        balances[to] = balances[to].add(tokens);
        Transfer(from, to, tokens);
        return true;
    }``
        - Line 98, 5 ``function allowance(address tokenOwner, address spender) public constant returns (uint remaining) {
        return allowed[tokenOwner][spender];
    }``
        - Line 101, 5 ``function approveAndCall(address spender, uint tokens, bytes data) public returns (bool success) {
        allowed[msg.sender][spender] = tokens;
        Approval(msg.sender, spender, tokens);
        ApproveAndCallFallBack(spender).receiveApproval(msg.sender, tokens, this, data);
        return true;
    }``
        - Line 120, 5 ``function transferAnyERC20Token(address tokenAddress, uint tokens) public onlyOwner returns (bool success) {
        return ERC20Interface(tokenAddress).transfer(owner, tokens);
    }``

* __O__ TOD
* _`X`_ TimeDep
    - 100%, REVERT JUMPDEST PUSH PUSH JUMP

        - Line 35, 5 ``address public owner``
        - Line 36, 5 ``address public newOwner``
        - Line 48, 5 ``function acceptOwnership() public {
        require(msg.sender == newOwner);
        OwnershipTransferred(owner, newOwner);
        owner = newOwner;
        newOwner = address(0);
    }``
        - Line 57, 5 ``string public symbol``
        - Line 58, 5 ``string public  name``
        - Line 59, 5 ``uint8 public decimals``
        - Line 60, 5 ``uint public _totalSupply``
        - Line 61, 5 ``uint public startDate``
        - Line 62, 5 ``uint public bonusEnds``
        - Line 63, 5 ``uint public endDate``
        - Line 74, 5 ``function totalSupply() public constant returns (uint) {
        return _totalSupply  - balances[address(0)];
    }``

* __O__ Reentrancy
* _`X`_ AssertFail
    - 100%, PUSH GAS SUB DELEGATECALL ISZERO

        - Line 81, 32 ``balances[msg.sender].sub(tokens)``
        - Line 82, 24 ``balances[to].add(tokens)``
        - Line 92, 26 ``balances[from].sub(tokens)``
        - Line 93, 37 ``allowed[from][msg.sender].sub(tokens)``
        - Line 94, 24 ``balances[to].add(tokens)``
        - Line 115, 32 ``balances[msg.sender].add(tokens)``
        - Line 116, 24 ``_totalSupply.add(tokens)``

* __O__ TxOrigin
* __O__ CheckEffects
* _`X`_ InlineAssembly
    - 100%, PUSH GAS SUB DELEGATECALL ISZERO

        - Line 81, 32 ``balances[msg.sender].sub(tokens)``
        - Line 82, 24 ``balances[to].add(tokens)``
        - Line 92, 26 ``balances[from].sub(tokens)``
        - Line 93, 37 ``allowed[from][msg.sender].sub(tokens)``
        - Line 94, 24 ``balances[to].add(tokens)``
        - Line 115, 32 ``balances[msg.sender].add(tokens)``
        - Line 116, 24 ``_totalSupply.add(tokens)``

* _`X`_ BlockTimestamp
    - 100%, TIMESTAMP

        - Line 108, 17 ``now``
        - Line 108, 37 ``now``
        - Line 110, 13 ``now``

* _`X`_ LowlevelCalls
    - 100%, DELEGATECALL ISZERO

        - Line 81, 32 ``balances[msg.sender].sub(tokens)``
        - Line 82, 24 ``balances[to].add(tokens)``
        - Line 92, 26 ``balances[from].sub(tokens)``
        - Line 93, 37 ``allowed[from][msg.sender].sub(tokens)``
        - Line 94, 24 ``balances[to].add(tokens)``
        - Line 115, 32 ``balances[msg.sender].add(tokens)``
        - Line 116, 24 ``_totalSupply.add(tokens)``

* __O__ BlockHash
* _`X`_ SelfDestruct
    - 100%, SWAP SWAP POP POP PUSH

        - Line 4, 5 ``function add(uint a, uint b) public pure returns (uint c) {
        c = a + b;
        require(c >= a);
    }``
        - Line 8, 5 ``function sub(uint a, uint b) public pure returns (uint c) {
        require(b <= a);
        c = a - b;
    }``
        - Line 12, 5 ``function mul(uint a, uint b) public pure returns (uint c) {
        c = a * b;
        require(a == 0 || c / a == b);
    }``
        - Line 16, 5 ``function div(uint a, uint b) public pure returns (uint c) {
        require(b > 0);
        c = a / b;
    }``
        - Line 45, 5 ``function transferOwnership(address _newOwner) public onlyOwner {
        newOwner = _newOwner;
    }``
        - Line 77, 5 ``function balanceOf(address tokenOwner) public constant returns (uint balance) {
        return balances[tokenOwner];
    }``
        - Line 80, 5 ``function transfer(address to, uint tokens) public returns (bool success) {
        balances[msg.sender] = balances[msg.sender].sub(tokens);
        balances[to] = balances[to].add(tokens);
        Transfer(msg.sender, to, tokens);
        return true;
    }``
        - Line 86, 5 ``function approve(address spender, uint tokens) public returns (bool success) {
        allowed[msg.sender][spender] = tokens;
        Approval(msg.sender, spender, tokens);
        return true;
    }``
        - Line 91, 5 ``function transferFrom(address from, address to, uint tokens) public returns (bool success) {
        balances[from] = balances[from].sub(tokens);
        allowed[from][msg.sender] = allowed[from][msg.sender].sub(tokens);
        balances[to] = balances[to].add(tokens);
        Transfer(from, to, tokens);
        return true;
    }``
        - Line 98, 5 ``function allowance(address tokenOwner, address spender) public constant returns (uint remaining) {
        return allowed[tokenOwner][spender];
    }``
        - Line 101, 5 ``function approveAndCall(address spender, uint tokens, bytes data) public returns (bool success) {
        allowed[msg.sender][spender] = tokens;
        Approval(msg.sender, spender, tokens);
        ApproveAndCallFallBack(spender).receiveApproval(msg.sender, tokens, this, data);
        return true;
    }``
        - Line 120, 5 ``function transferAnyERC20Token(address tokenAddress, uint tokens) public onlyOwner returns (bool success) {
        return ERC20Interface(tokenAddress).transfer(owner, tokens);
    }``

