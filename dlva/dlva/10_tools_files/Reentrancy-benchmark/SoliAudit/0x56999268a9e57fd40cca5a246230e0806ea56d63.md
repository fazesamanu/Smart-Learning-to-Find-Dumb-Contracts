# Vulnerability Analysis #
#### 2023-05-29 09:13:28 ####

* __O__ Underflow
* _`X`_ Overflow
    - 100%, SWAP SWAP SWAP SWAP SWAP

        - Line 5, 5 ``function deploy(bytes calldata contractBytecode, bytes32 salt) public {
    bytes memory bytecode = contractBytecode;
    address addr;
    assembly {
        addr := create2(0, add(bytecode, 0x20), mload(bytecode), salt)
    }
    naddr = addr;
    }``

* __O__ Multisig
* __O__ CallDepth
* __O__ TOD
* __O__ TimeDep
* __O__ Reentrancy
* _`X`_ AssertFail
    - 100%, REVERT JUMPDEST SWAP SWAP SWAP

        - Line 5, 5 ``function deploy(bytes calldata contractBytecode, bytes32 salt) public {
    bytes memory bytecode = contractBytecode;
    address addr;
    assembly {
        addr := create2(0, add(bytecode, 0x20), mload(bytecode), salt)
    }
    naddr = addr;
    }``

* __O__ TxOrigin
* __O__ CheckEffects
* _`X`_ InlineAssembly
    - 100%, MLOAD PUSH DUP ADD PUSH

        - Line 9, 49 ``mload(bytecode)``

* __O__ BlockTimestamp
* _`X`_ LowlevelCalls
    - 100%, PUSH CREATE

        - Line 9, 25 ``0``

* __O__ BlockHash
* __O__ SelfDestruct
