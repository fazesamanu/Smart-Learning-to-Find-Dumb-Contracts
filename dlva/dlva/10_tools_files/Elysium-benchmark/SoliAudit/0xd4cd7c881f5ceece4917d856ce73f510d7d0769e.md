# Vulnerability Analysis #
#### 2023-05-29 11:50:49 ####

* _`X`_ Underflow
    - 31%, NOT DUP

        - Line 17, 1 ``  }
  
  ``

    - 25%, SLOAD DIV

        - Line 75, 5 ``string public name = "DecentralizedExchanges"``

    - 23%, PUSH NOT

        - Line 92, 5 ``mapping (bytes32 => OrderInfo) public orderInfos``
        - Line 93, 5 ``mapping (address => bytes32[]) public userOrders``
        - Line 94, 5 ``function getOrderInfo(bytes32 hash) public view returns (bool, uint, address, uint, uint, uint, address[], uint, address, uint, bool) {
        OrderInfo storage info = orderInfos[hash];
        return (info.isSell, info.eth, info.token, info.amount, info.expires, info.nonce, info.limitUser, info.createdAt, info.owner, info.fill, info.isSpecialERC20);
    }``
        - Line 95, 34 ``orderInfos[hash]``
        - Line 101, 9 ``orderInfos[hash]``
        - Line 103, 21 ``userOrders[msg.sender][i] != hash``
        - Line 105, 9 ``userOrders[msg.sender].push(hash)``
        - Line 106, 14 ``Order(hash)``
        - Line 111, 9 ``orderInfos[hash]``
        - Line 113, 21 ``userOrders[msg.sender][i] != hash``
        - Line 115, 9 ``userOrders[msg.sender].push(hash)``
        - Line 116, 14 ``Order(hash)``
        - Line 118, 5 ``function cancelOrder(bytes32 hash) public {
        OrderInfo storage info = orderInfos[hash];
        require(info.owner == msg.sender);
        if (info.isSell) {
            if (info.fill < info.amount) {
                uint amount = info.amount;
                uint remain = amount - info.fill;
                info.fill = info.amount;
                if (info.isSpecialERC20) {
                    SpecialERC20(info.token).transfer(msg.sender, remain);
                } else {
                    ERC20(info.token).transfer(msg.sender, remain);
                }
                emit Cancel(hash, remain, info.isSell);
            } else {
                emit Cancel(hash, 0, info.isSell);
            }
        } else {
            if (info.fill < info.eth) {
                uint eth = info.eth;
                remain = eth - info.fill;
                info.fill = info.eth;
                msg.sender.transfer(eth);
                emit Cancel(hash, remain, info.isSell);
            } else {
                emit Cancel(hash, 0, info.isSell);
            }
        }
    }``
        - Line 119, 34 ``orderInfos[hash]``
        - Line 131, 22 ``Cancel(hash, remain, info.isSell)``
        - Line 133, 22 ``Cancel(hash, 0, info.isSell)``
        - Line 141, 22 ``Cancel(hash, remain, info.isSell)``
        - Line 143, 22 ``Cancel(hash, 0, info.isSell)``
        - Line 147, 5 ``function sell(bytes32 hash, uint amount) public {
        OrderInfo storage info = orderInfos[hash];
        bool find = false;
        if (info.limitUser.length > 0) {
            for (uint i = 0; i < info.limitUser.length; i++) {
                if (info.limitUser[i] == msg.sender) {
                    find = true;
                    break;
                }
            }
            require(find);
        }
        require(info.fill < info.eth);
        require(info.expires >= now);
        uint remain = info.eth - info.fill;
        uint remainAmount = remain.mul(info.amount).div(info.eth);
        uint tradeAmount = remainAmount < amount ? remainAmount : amount;
        ERC20(info.token).safeTransferFrom(msg.sender, this, tradeAmount);
        uint total = info.eth.mul(tradeAmount).div(info.amount);
        msg.sender.transfer(total);
        if (info.isSpecialERC20) {
            SpecialERC20(info.token).transfer(info.owner, tradeAmount);
        } else {
            ERC20(info.token).transfer(info.owner, tradeAmount);
        }
        info.fill = info.fill.add(total);
        emit Trade(hash, msg.sender, info.token, tradeAmount, info.owner, total);
    }``
        - Line 148, 34 ``orderInfos[hash]``
        - Line 173, 14 ``Trade(hash, msg.sender, info.token, tradeAmount, info.owner, total)``
        - Line 175, 5 ``function purchase(bytes32 hash, uint amount) payable public {
        OrderInfo storage info = orderInfos[hash];
        bool find = false;
        if (info.limitUser.length > 0) {
            for (uint i = 0; i < info.limitUser.length; i++) {
                if (info.limitUser[i] == msg.sender) {
                    find = true;
                    break;
                }
            }
            require(find);
        }
        require(info.fill < info.amount);
        require(info.expires >= now);
        uint remainAmount = info.amount - info.fill;
        uint tradeAmount = remainAmount < amount ? remainAmount : amount;
        uint total = info.eth.mul(tradeAmount).div(info.amount);
        require(msg.value >= total);
        if (msg.value > total) {
            msg.sender.transfer(msg.value - total);
        }
        info.owner.transfer(total);
        if (info.isSpecialERC20) {
            SpecialERC20(info.token).transfer(msg.sender, tradeAmount);
        } else {
            ERC20(info.token).transfer(msg.sender, tradeAmount);
        }
        info.fill = info.fill.add(tradeAmount);
        emit Trade(hash, info.owner, info.token, tradeAmount, msg.sender, total);
    }``
        - Line 176, 34 ``orderInfos[hash]``
        - Line 203, 14 ``Trade(hash, info.owner, info.token, tradeAmount, msg.sender, total)``

    - 21%, ADD SLOAD

        - Line 92, 5 ``mapping (bytes32 => OrderInfo) public orderInfos``
        - Line 96, 30 ``info.eth``
        - Line 96, 52 ``info.amount``
        - Line 96, 65 ``info.expires``
        - Line 96, 79 ``info.nonce``
        - Line 96, 107 ``info.createdAt``
        - Line 96, 135 ``info.fill``
        - Line 103, 21 ``userOrders[msg.sender][i]``
        - Line 113, 21 ``userOrders[msg.sender][i]``
        - Line 122, 17 ``info.fill``
        - Line 122, 29 ``info.amount``
        - Line 123, 31 ``info.amount``
        - Line 124, 40 ``info.fill``
        - Line 125, 29 ``info.amount``
        - Line 136, 17 ``info.fill``
        - Line 136, 29 ``info.eth``
        - Line 137, 28 ``info.eth``
        - Line 138, 32 ``info.fill``
        - Line 139, 29 ``info.eth``
        - Line 159, 17 ``info.fill``
        - Line 159, 29 ``info.eth``
        - Line 160, 17 ``info.expires``
        - Line 161, 23 ``info.eth``
        - Line 161, 34 ``info.fill``
        - Line 162, 40 ``info.amount``
        - Line 162, 57 ``info.eth``
        - Line 165, 22 ``info.eth``
        - Line 165, 52 ``info.amount``
        - Line 172, 21 ``info.fill``
        - Line 187, 17 ``info.fill``
        - Line 187, 29 ``info.amount``
        - Line 188, 17 ``info.expires``
        - Line 189, 29 ``info.amount``
        - Line 189, 43 ``info.fill``
        - Line 191, 22 ``info.eth``
        - Line 191, 52 ``info.amount``
        - Line 202, 21 ``info.fill``

* _`X`_ Overflow
    - 100%, SWAP SWAP SWAP SWAP SWAP

        - Line 94, 5 ``function getOrderInfo(bytes32 hash) public view returns (bool, uint, address, uint, uint, uint, address[], uint, address, uint, bool) {
        OrderInfo storage info = orderInfos[hash];
        return (info.isSell, info.eth, info.token, info.amount, info.expires, info.nonce, info.limitUser, info.createdAt, info.owner, info.fill, info.isSpecialERC20);
    }``
        - Line 98, 5 ``function createPurchaseOrder(bool isSpecialERC20, uint eth, address token, uint amount, uint expires, address[] seller, uint nonce) payable public {
        require(msg.value >= eth);
        bytes32 hash = sha256(abi.encodePacked(this, eth, token, amount, expires, seller, nonce, msg.sender, now));
        orderInfos[hash] = OrderInfo(false, isSpecialERC20, eth, amount, expires, nonce, now, 0, token, seller, msg.sender);
        for (uint i = 0; i < userOrders[msg.sender].length; i++) {
            require(userOrders[msg.sender][i] != hash);
        }
        userOrders[msg.sender].push(hash);
        emit Order(hash);
    }``
        - Line 105, 9 ``userOrders[msg.sender].push(hash)``
        - Line 108, 5 ``function createSellOrder(bool isSpecialERC20, address token, uint amount, uint eth, uint expires, address[] purchaser, uint nonce) public {
        ERC20(token).safeTransferFrom(msg.sender, this, amount);
        bytes32 hash = sha256(abi.encodePacked(this, eth, token, amount, expires, purchaser, nonce, msg.sender, now));
        orderInfos[hash] = OrderInfo(true, isSpecialERC20, eth, amount, expires, nonce, now, 0, token, purchaser, msg.sender);
        for (uint i = 0; i < userOrders[msg.sender].length; i++) {
            require(userOrders[msg.sender][i] != hash);
        }
        userOrders[msg.sender].push(hash);
        emit Order(hash);
    }``
        - Line 109, 9 ``ERC20(token).safeTransferFrom``
        - Line 115, 9 ``userOrders[msg.sender].push(hash)``
        - Line 164, 9 ``ERC20(info.token).safeTransferFrom``

* __O__ Multisig
* __O__ CallDepth
* _`X`_ TOD
    - 100%, DUP DUP SUB SUB DUP

        - Line 4, 13 ``Math {
``

* __O__ TimeDep
* __O__ Reentrancy
* __O__ AssertFail
* __O__ TxOrigin
* _`X`_ CheckEffects
    - 100%, NOT AND PUSH PUSH CALLER

        - Line 103, 21 ``userOrders[msg.sender][i] != hash``
        - Line 113, 21 ``userOrders[msg.sender][i] != hash``

* __O__ InlineAssembly
* _`X`_ BlockTimestamp
    - 100%, TIMESTAMP

        - Line 100, 110 ``now``
        - Line 101, 90 ``now``
        - Line 110, 113 ``now``
        - Line 111, 89 ``now``
        - Line 160, 33 ``now``
        - Line 188, 33 ``now``

* __O__ LowlevelCalls
* __O__ BlockHash
* __O__ SelfDestruct
