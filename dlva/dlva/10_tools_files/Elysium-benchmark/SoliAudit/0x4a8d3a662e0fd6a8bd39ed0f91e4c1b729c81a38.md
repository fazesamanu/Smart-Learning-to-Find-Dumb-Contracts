# Vulnerability Analysis #
#### 2023-05-29 11:48:29 ####

* __O__ Underflow
* _`X`_ Overflow
    - 100%, JUMPDEST JUMP JUMPDEST PUSH CALLER

        - Line 65, 9 ``doDeposit(msg.sender)``

* __O__ Multisig
* _`X`_ CallDepth
    - 100%, SWAP SWAP POP POP PUSH

        - Line 33, 5 ``mapping(address => uint256) public balances``
        - Line 34, 5 ``mapping(address => uint256) public withdrawalCount``
        - Line 35, 5 ``mapping(address => mapping(address => bool)) public approvals``
        - Line 67, 5 ``function depositTo(address to) payable public {
        doDeposit(to);
    }``
        - Line 70, 5 ``function withdraw(uint256 amount) public {
        doWithdraw(msg.sender, msg.sender, amount);
    }``
        - Line 73, 5 ``function withdrawTo(address to, uint256 amount) public {
        doWithdraw(msg.sender, to, amount);
    }``
        - Line 76, 5 ``function withdrawFor(address from, uint256 amount) public {
        require(approvals[from][msg.sender]);
        doWithdraw(from, msg.sender, amount);
    }``
        - Line 80, 5 ``function withdrawForTo(address from, address to, uint256 amount) public {
        require(approvals[from][msg.sender]);
        doWithdraw(from, to, amount);
    }``
        - Line 87, 5 ``function getBalance(address toCheck) public constant returns (uint256) {
        return balances[toCheck];
    }``
        - Line 90, 5 ``function addBalances(address[] addrs, uint256[] _balances) public payable onlyOwner {
        require(addrs.length == _balances.length);
        for (uint256 i = 0; i < addrs.length; i++) {
            balances[addrs[i]] = _balances[i];
            withdrawalCount[addrs[i]] = 0;
        }
    }``
        - Line 97, 5 ``function approve(address toApprove) public {
        require(balances[msg.sender] > 0);
        approvals[msg.sender][toApprove] = true;
    }``
        - Line 101, 5 ``function unapprove(address toUnapprove) public {
        require(balances[msg.sender] > 0);
        approvals[msg.sender][toUnapprove] = false;
    }``

* __O__ TOD
* __O__ TimeDep
* __O__ Reentrancy
* __O__ AssertFail
* __O__ TxOrigin
* __O__ CheckEffects
* __O__ InlineAssembly
* __O__ BlockTimestamp
* _`X`_ LowlevelCalls
    - 100%, GAS CALL

        - Line 54, 9 ``to.call.value(amount)()``

* __O__ BlockHash
* _`X`_ SelfDestruct
    - 100%, SWAP SWAP POP POP PUSH

        - Line 33, 5 ``mapping(address => uint256) public balances``
        - Line 34, 5 ``mapping(address => uint256) public withdrawalCount``
        - Line 35, 5 ``mapping(address => mapping(address => bool)) public approvals``
        - Line 67, 5 ``function depositTo(address to) payable public {
        doDeposit(to);
    }``
        - Line 70, 5 ``function withdraw(uint256 amount) public {
        doWithdraw(msg.sender, msg.sender, amount);
    }``
        - Line 73, 5 ``function withdrawTo(address to, uint256 amount) public {
        doWithdraw(msg.sender, to, amount);
    }``
        - Line 76, 5 ``function withdrawFor(address from, uint256 amount) public {
        require(approvals[from][msg.sender]);
        doWithdraw(from, msg.sender, amount);
    }``
        - Line 80, 5 ``function withdrawForTo(address from, address to, uint256 amount) public {
        require(approvals[from][msg.sender]);
        doWithdraw(from, to, amount);
    }``
        - Line 87, 5 ``function getBalance(address toCheck) public constant returns (uint256) {
        return balances[toCheck];
    }``
        - Line 90, 5 ``function addBalances(address[] addrs, uint256[] _balances) public payable onlyOwner {
        require(addrs.length == _balances.length);
        for (uint256 i = 0; i < addrs.length; i++) {
            balances[addrs[i]] = _balances[i];
            withdrawalCount[addrs[i]] = 0;
        }
    }``
        - Line 97, 5 ``function approve(address toApprove) public {
        require(balances[msg.sender] > 0);
        approvals[msg.sender][toApprove] = true;
    }``
        - Line 101, 5 ``function unapprove(address toUnapprove) public {
        require(balances[msg.sender] > 0);
        approvals[msg.sender][toUnapprove] = false;
    }``

